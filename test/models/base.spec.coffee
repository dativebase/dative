define (require) ->

  BaseModel = require('../../../scripts/models/base')

  describe 'Base Model', ->

    # TODO: write XHR and CORS tests.

    it 'can get an XHR object', ->

    it 'can perform a CORS request', ->

